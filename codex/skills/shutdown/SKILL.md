---
name: shutdown
description: Alias for the $goodnight skill
---

# Shutdown

Read `~/.codex/skills/goodnight/SKILL.md` completely, then execute it in full. Treat any arguments passed to `$shutdown` as arguments to `$goodnight`.
