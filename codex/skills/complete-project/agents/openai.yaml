interface:
  display_name: "Complete Project"
  short_description: "Close a project and route its artefacts"
  default_prompt: "Use $complete-project to close this project and route its artefacts."
policy:
  allow_implicit_invocation: false
