---
name: verify-provenance
description: "Deprecated: provenance verification is now part of $weekly-hygiene step 13b"
---


# Verify Provenance — Redirected

Provenance verification has been absorbed into `$weekly-hygiene` (step 13b). Run `$weekly-hygiene` instead. To run only the provenance check, read `~/.codex/skills/weekly-hygiene/SKILL.md` completely and execute step 0 (vault path resolution — step 13b's code uses the `{VAULT}` placeholder and will not run without it) followed by step 13b.
