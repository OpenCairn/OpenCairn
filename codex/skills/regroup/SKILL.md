---
name: regroup
description: Alias for the $afternoon skill
---

# Regroup

Read `~/.codex/skills/afternoon/SKILL.md` completely, then execute it in full. Treat any arguments passed to `$regroup` as arguments to `$afternoon`.
