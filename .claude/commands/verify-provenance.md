---
name: verify-provenance
description: "Deprecated: provenance verification is now part of /weekly-hygiene step 14b"
---

# Verify Provenance — Redirected

Provenance verification has been absorbed into `/weekly-hygiene` (step 14b). Run `/weekly-hygiene` instead. To run only the provenance check, read `weekly-hygiene.md` from this skill's own commands directory and execute step 0 (vault path resolution — step 14b's code uses the `{VAULT}` placeholder and will not run without it) followed by step 14b.
